using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Horse : Yari
{
    // Start is called before the first frame update
    void Start()
    {
        mobility = 2;
        hitPoints = 1800;
        prov = 18000;
        eatSpeed = 2;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
