using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Gun : Yari
{
    // Start is called before the first frame update
    void Start()
    {
        //初期設定
        mobility = 1;
        hitPoints = 1500;
        prov = 15000;
}

    // Update is called once per frame
    void Update()
    {
        
    }
}
